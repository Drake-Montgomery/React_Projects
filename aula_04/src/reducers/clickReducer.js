import {CLICK_UPDATE_VALUE} from '../actions/actionTypes'

const initiaState = {

    newValue: ''

    };

    export const clickReducer = (state = inicialState, action) =>
        switch (action.type) {
            case CLICK_UPDATE_VALUE:
        }