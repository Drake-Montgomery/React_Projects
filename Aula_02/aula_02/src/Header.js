import React from "react";

function Header(props){
    return(
        <header>

            <h1>{props.title}</h1>
            <h1>hakuna Matata Motto</h1>
        </header>
    );
}

export default Header;